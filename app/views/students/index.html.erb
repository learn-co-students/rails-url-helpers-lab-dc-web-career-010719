<div>
  <% @students.each do |student| %>
    <div><%= link_to student, student_path(student) %></div>
    <%# <div><%= link_to student.to_s, controller: "students", action: "show", id: student %></div>
  <% end %>
</div>